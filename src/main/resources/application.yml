# HTTP Server
server:
  port: 8080

management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    health:
      show-details: always

#Server info
info:
  app:
    name: ${spring.application.name}
    description: Football Team Management API
    groupId: com.torresj
    artifactId: football-team-management-api
    version: ${APP_VERSION:SNAPSHOT}
